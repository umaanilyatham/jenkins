pipeline {
    agent any

    stages {

        stage('Checkout Code') {
            steps {
               echo "running tests"
            }
        }

        stage('Run Ansible Playbook') {
            steps {
               echo "tested"
            }
        }

    }
}
